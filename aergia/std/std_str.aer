# standard string library
# by las-r

{join :arr del:
    = out ""
    = first 1
    [`arr itm
        (== first 1
            = out itm
            = first 0
        ) -> (
            =+ out +del itm
        )
    ]
    ? out
}

{split :str del:
    = out < >
    = cur ""
    [`str chr
        (!= del ""
            (== chr del
                +: out cur
                = cur ""
            ) -> (
                =+ cur chr
            )
        ) -> (
            =+ out chr
        )
    ]
    +: out cur
    ? out
}

{has :str seq:
    = matchidx 0
    = found 0
    [`str chr
        = target ""
        = idxcheck 0
        [`seq schr
            (== idxcheck matchidx
                = target schr
            )
            =+ idxcheck 1
        ]
        (== chr target
            =+ matchidx 1
        ) -> (
            = matchidx 0
        )
        (== target ""
            = found 1
        )
    ]
    ? found
}

{reverse :str:
    = out <>
    [`str chr
        *: out 0 chr
    ]
    ? @str_join:out "":
}

{ltrim :str:
    = out ""
    = start 0
    = end 0
    [`str chr
        (!start
            (!= chr " "
                = start 1
            )
        ) -> (
            =+ out chr
        )
    ]
    ? out
}
{rtrim :str: 
    ? @str_ltrim:@str_reverse:str::
}
{trim :str:
    ? @str_ltrim:@str_rtrim:str::
}