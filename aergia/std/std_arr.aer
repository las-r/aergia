# standard array library
# by las-r
# with functions from lyxal

{arr_has :arr itm:
    [`arr aitm
        (== aitm itm
            ? 1
        )
    ]
    ? 0
}

{arr_reverse :arr:
    = out <>
    [`arr itm
        *: out 0 itm
    ]
    ? out
}

{arr_index :arr tar:
    = idx 0
    [`arr itm
        (== itm tar
            ? idx
        )
        =+ idx 1
    ]
    ? -0 1
}

# by lyxal
{arr_map :arr fn:
    = out <>
    = fn +"{ f :x:" +fn "} @f:arg:"
    [`arr arg
        +: out ; fn
    ] 
    ? out
}

# by lyxal
{arr_filter :arr fn:
    = out <>
    = fn +"{ f :x:" +fn "} @f:arg:"
    [`arr arg
        (; fn
            +: out arg
        )
    ]
    ? out
}

# based off lyxal
{arr_reduce :arr ini fn:
    = acc ini
    = fn +"{ f :x y:" +fn "} @f:acc curr:"
    [`arr curr
        = acc ; fn
    ]
    ? acc
}

# based off lyxal
{arr_iota :n:
    = out <>
    = i 0
    [<< i n
        +: out i
        =+ i 1
    ]
    ? out
}