To install Aergia support for Sublime Text manually:

1. Open Sublime Text and go to **Preferences > Browse Packages...**
2. Create a new folder named `Aergia` inside that directory.
3. Copy the contents of this `editors/sublime/` directory and paste them into that new `Aergia` folder.